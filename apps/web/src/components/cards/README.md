# cards

Composed card patterns above the `Card` primitive in `@apex/ui`: metric cards,
entity summary cards, action cards.

The primitive stays in the design system; domain-flavoured compositions live
here.
