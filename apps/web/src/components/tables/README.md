# tables

Data table shell: column definitions, sorting, pagination controls, row
selection, empty and loading states.

Tables consume the cursor-pagination contract from `@apex/types`, so every list
in the product paginates the same way.
