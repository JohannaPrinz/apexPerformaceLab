# layout

Page-level structure: app shell, sidebar/content split, page headers, section
containers, footers.

Layout components own **spacing and structure only** — no data fetching, no
business logic. That keeps them reusable across every route group.
