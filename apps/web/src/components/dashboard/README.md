# dashboard

Reusable dashboard building blocks: KPI tiles, stat groups, activity feeds,
empty states.

These are _presentational_ and generic. Anything specific to one dashboard view
belongs in `features/dashboard/components` instead.
